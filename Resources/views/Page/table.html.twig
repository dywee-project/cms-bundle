{# Dywee\CMSBundle\Resources\View\CMS\table.html.twig #}

{% extends '@DyweeCMSBundle/admin.html.twig' %}

{% block metaTitle %}
    Liste de vos pages {{ parent() }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">

            <div class="box box-defaut">
                <div class="box-header with-border">
                    <div class="box-tools">
                        <a href="{{ path('page_add') }}" class="btn btn-success btn-box-tool"><i class="fa fa-plus"></i> Ajouter</a>
                    </div>
                    <h1 class="box-title">Liste des Pages</h1>
                </div>
                {% include '@DyweeCMSBundle/Page/miniTable.html.twig' %}
            </div>

        </div>
    </div>

{% endblock %}

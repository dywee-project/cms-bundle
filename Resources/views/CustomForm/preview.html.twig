{% extends 'DyweeCMSBundle::admin.html.twig' %}

{% block body %}

    <div class="box">
        <div class="box-header with-border">
            <h1 class="box-title">Préview du formulaire {{ customForm.name }}</h1>
        </div>
        <div class="box-body">
            {{ form(form) }}
        </div>
    </div>

{% endblock %}
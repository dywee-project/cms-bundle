{{ form_start(form) }}
{{ form_rest(form) }}

<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <button type="submit" id="cms_alert_save" class="btn-success btn-block btn">
            <i class="fa fa-check"></i> Valider
        </button>
    </div>
</div>

{{ form_end(form) }}